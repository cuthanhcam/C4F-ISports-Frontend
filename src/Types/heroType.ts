export interface heroType {
    id: number;
    name: string;
};

export interface heroSearchCityType {
    id: number;
    nameCity: string;
};

export interface heroSearchDistrictType {
    id: number;
    nameDistrict: string;
};

export interface heroSearchChiocePlayType {
    id: number;
    image: string;
    name: string;
};