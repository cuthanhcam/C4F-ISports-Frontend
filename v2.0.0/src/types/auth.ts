export interface authLogin {
    email: string;
    password: string;
};

export interface authRegister {
    fullName: string;
    email: string;
    password: string;
    confirmPassword: string;
    role: string;
    phone: string;
};

