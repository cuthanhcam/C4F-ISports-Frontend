export interface authLogin {
    email: string;
    password: string;
};

export interface authRegister {
    email: string;
    password: string;
    confirmPassword: string;
    role: string;
    fullName: string;
    phone: string;
};

